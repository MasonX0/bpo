### **изменение приоритета**

```
#include <unistd.h>
int nice(int inc);

```

**Увеличивает "niceness"** (меньше значение = выше приоритет):

- `inc > 0` — понижает приоритет (процесс "nicer")
    
- `inc < 0` — повышает приоритет (только root)