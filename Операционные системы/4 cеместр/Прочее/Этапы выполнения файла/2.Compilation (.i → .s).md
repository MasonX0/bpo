### **Компиляция в ассемблер**

```
gcc -S example.i -o example.s
# или напрямую
gcc -S example.c -o example.s

```

**Что происходит:**

- Синтаксический анализ
    
- Семантический анализ
    
- Оптимизация
    
- Генерация ассемблерного кода

**Программа:** `cc1` (компилятор C)