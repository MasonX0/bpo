каталоги \ mnt 
системный вызов mount() при вставлении флешки

при монтировании каталого закрывается доступ в каталогу?

mount \ unmount     - cпрашивание при извлечении флешка





дефениций файлов \ модификации
файл - именованная область на диске
в UNIX почти все - файл


каталог - файл, стркутура которого предопределена


open\opendir

i-nodes, файловый дескриптор
./..
cndir ..




несколько имен одного файла 
hard link               vs          soft link
1) софт - ссылка, ярлык ( в винде) - создается i-nodes, можно делать ярлыки к каталогу; можно на другие ФС (флешки и тд); не увеличивает счетчик
2) хард - другое имя файла (не создается индексного дискриптора inodes), жесткую ссылку на каталог может делать только рут - нарушение файловой системы (дерева) - превращается в граф, если ссылка ссылается на каталог выше текущего; нельзя ссылаться на другие файловые системы; это счетчик использования в винде  - увеличивает счетчик использования - про удаление файлов при счетчике 0 
   
   link / ulink - все файлы удаляются так - сектора обнуляются





пользователи в контексте прав на файл:
1) Владелец 
2) Участник группы
3) Остальные

0400 - чтение
про кодировку, ортонормальное кодирование, почему 0,1,2,4 а 3 - нет? расскладывание в обратную сторону

0007 =? 0777

0741?
про права доступа к каталогом 
про файловую систему мою

userid 3 штуки есть у каждого пользователя
права доступа: хранимые, реальные, фиктивные
про pswd и повышение доступа на время выполнения программы 




пока все


о создании дочерних процессов (2 ЛР):
**виртуальное адресное пространство родителя и дочернего (при fork)

дочерний процесс наследует родительские идентификаторы, текущий каталог, рутовый каталог, nice()

различаются  с помощью PPID, PID, блокировки файлов, рисовка генеалогического дерева с помощью PPID

про роль exec() -  написан  у родителя, а выполняется у дочернего; при ошибке убивает дочерний процесс и возвращает код ошибки родителю? засыпание на вейте

парные вызовы - fork -> exec  and exit-wait()?????


1. если родительский процесс теряется (умирает \\\) -> дочерний становится сиротой -> всех усыновляет init()  (чтоб вызвать wait (родителя?)? -> и окончательно завершить их процесс)

2. родительский процесс без вейта продолжает работать -> потомок посылает свой exit -> ???



прототипы функций: 


1. хороший тон - проверять статусы ошибок (if pidf!= / = -1)  +errno 
2. 

















































